<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only: allows plain HTTP (cleartext) to the local Laravel dev
  server. Android 9+ blocks cleartext traffic by default, and 10.0.2.2 is the
  Android emulator's alias for the host machine's 127.0.0.1 (see
  FLUTTER_SETUP.md). This file is NOT included in release builds — production
  API URLs must be HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
